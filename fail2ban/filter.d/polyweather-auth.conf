# PolyWeather authentication failure filter

[Definition]
failregex = ^<HOST> - .* "POST /auth/.* HTTP/.*" 401 .*$
            ^<HOST> - .* "POST /auth/.* HTTP/.*" 403 .*$
            ^<HOST> - .* "POST /api/auth/.* HTTP/.*" 401 .*$
            ^<HOST> - .* "POST /api/auth/.* HTTP/.*" 403 .*$

ignoreregex =